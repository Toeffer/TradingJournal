# Placeholders only — never commit real credentials.
# For GitHub Actions, set these as repository secrets instead (see README).
APCA_API_KEY_ID=your-alpaca-key-id
APCA_API_SECRET_KEY=your-alpaca-secret-key
