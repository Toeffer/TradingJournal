# Weekly Research and Decision Review — 2026-08-23 (21:38 UTC run)
DRAFT for human review. Not financial advice.

> Second same-day run. It does not replace `research/candidates-2026-08-23.md` (17:01 UTC,
> GPT-5.6 Sol); both are retained per the repeated-run rule in `research_method/output_schema.md`.

## Run metadata
- Model / research mode: claude-opus-5 (Claude Code, scheduled run) / continuity-first catalyst-first
- Input snapshot / SHA-256: `data/research_snapshot.csv` / `1bbac2049352c0d225b6e1c8e2fa8ffe75e8f6d3a824795b1a47c0c48c8c52d0`
- Snapshot rebuilt at 2026-08-23 21:32 UTC via `build_research_snapshot.py --archive`; **1 canonical row (HOOD)**, byte-identical to the 08-21 build.
- Prior reports reviewed: `candidates-2026-08-23.md`, `candidates-2026-08-16.md`, `candidates-2026-08-09.md` and matching manifests, plus the full recommendation registry and review ledger.
- Active recommendations reviewed: 0 (`REC-CELC-2026-0004` archived 2026-08-10).
- Open positions reviewed: 0 — `trades.csv` has no open rows.
- Preliminary pool: 12 leads; 5 XETRA (AT1.DE, DHER.DE, AFX.DE, NEM.DE, SHL.DE), 6 US, 1 ADR. Meets `MIN_EU_PRELIMINARY: 4`.
- New leads / Actionable / Early Watch / Reject counts: 12 / 0 / 0 / 12
- Weekly decision: **NO NEW TRADE**

## Run integrity — read this first

**Stage 2 verification was structurally unreachable this run. No primary source could be opened, so no candidate could legitimately be classified Actionable or Early Watch, regardless of merit.**

| Tool | Status |
|---|---|
| `WebFetch` | **Blocked for every domain tested** — issuer IR sites, sec.gov, data.sec.gov, businesswire.com, deutsche-boerse.com, eqs-news.com, finance.yahoo.com, even en.wikipedia.org. All return `EGRESS_BLOCKED`. |
| Direct HTTPS (`curl`) | **Blocked** — the proxy gateway answered `403` to `CONNECT` for every host. |
| Quartr MCP | **Unavailable** — `subscription_required`; the signed-in account (`chris.bloeffer@gmail.com`) has plan `none`. |
| FMP MCP | **Partial** — `company/profile-symbol` works and was the backbone of this run. `quote/*` denied (needs Premium). `chart/*` denied for every symbol except HOOD. `calendar/earnings-calendar` returns only ~6 symbols for a three-week window. |
| WebSearch | Works — but `RESEARCH_BRIEF.md` states a search snippet "cannot verify a final date". Lead generation only. |

This matters more than any individual name. `RESEARCH_BRIEF.md` requires that "the synthesizing pass must open the primary source itself", and
`validate_candidate_manifest.py` enforces `catalyst.source_type == "primary"` with an opened URL for every non-rejected candidate. With
zero reachable primary sources, the maximum achievable date status this week is `secondary_only` — which the brief classifies as Reject
until verified. **A NO NEW TRADE result this week is therefore not evidence that the market offered nothing. It is evidence that the
routine could not see.** Do not read this run as a market signal.

Two further data-pipeline problems, independent of the network:

1. **The scanner has effectively stopped producing breadth.** `data/scanner_signals.csv` holds 39 rows in total, with a nine-day gap
   between 2026-08-13 and 2026-08-21 and exactly one ticker inside the 7-day snapshot window. That is why the canonical snapshot is a
   single row for the second week running. `build_research_snapshot.py` is working correctly; its input is starving.
2. **`data/eu_quote_history.csv` is not wired into the snapshot.** It holds clean daily OHLCV for 46 XETRA names through 2026-08-21 —
   real multi-session structure — but `build_research_snapshot.py` only reads `scanner_signals.csv`, `market_regime.csv` and
   `finviz_watchlist.csv`. Last week's and this morning's reports both rejected European names for having "no current normalized
   multi-session structure", when 155 sessions of it were sitting in the repository unused. This run read it directly; see
   *European structure* below.

## Prior recommendation audit

| Ticker | First mentioned | Previous action | Since mention | Trigger result | New status | This week |
|---|---|---|---|---|---|---|
| CELC | 2026-08-09 | `remove` (archived 2026-08-10) | Linked trade 2026-0004 closed 2026-07-09 at 108.00, +34.32 EUR / R +4.04 | n/a — no trigger was ever set | `archived` (terminal, unchanged) | No action; terminal |

There are no non-terminal recommendation objects. No lifecycle row changes and no review rows are appended this week.

## Existing positions

None. `trades.csv` contains 18 rows, all `closed`. No `manage` recommendation is required, and nothing in this run infers any change
to `trades.csv`.

## Coming-week decision sheet

**NO NEW TRADE.**

No candidate holds a primary-source-verified catalyst, because none could be opened. Under `SETUPS.md` and `RISK_RULES.md` that alone
bars every name from `enter_if_triggered`. No name is promoted to `wait_pullback` or `monitor` either: `analysis.md` keeps rejected
objects out of the registry, and admitting unverified leads as durable recommendations would launder a tooling failure into research
state. The leads below are recorded in this report and its manifest so next week's run can pick them up as `prior_report` origins with
the verification work already scoped.

## European structure

Computed directly from `data/eu_quote_history.csv` (46 names, daily OHLCV through 2026-08-21). Context that no prior run has had:

- **The whole reachable XETRA tape is offered.** 44 of 46 names closed *below* their 20-day high. Only SHL.DE (+0.2%) and SAP.DE (+0.6%)
  closed above, and both are outside the core universe on size. There is no European breakout to buy this week — that is a genuine
  market observation, not a data gap.
- Median distance below the 52-week high across the 46 names is roughly 20%.
- Only four names combine a 20-day return above +10% with a close within 5% of the 20-day high: SHL.DE, NEM.DE, AFX.DE and SRT3.DE.
  All are addressed below or fail the size/liquidity overlay.

Had the verification gate been open, **AFX.DE** would have been the lead worth the most work: core-bucket cap, adequate liquidity,
+16.8% over 20 sessions and holding within 0.5% of its 20-day high. It still lacks any catalyst inside the 42-day horizon, so it would
most likely have landed at `monitor` rather than a trigger. That is the honest ceiling for this week's European book.

## New research

Full evidence, with per-claim sources and access timestamps, is in
`research/manifests/candidates-2026-08-23-2138.json`. Summarised by rejection cause:

### Failed only on the verification gate — carry these into next week first

- **S — SentinelOne, Inc.** (NYSE, USD). $21.19, cap $7.14B (core), ~$161M ADV. Fiscal Q2 2027 results reported as 2026-08-27 by a
  search summary of a Businesswire release; the release could not be opened. Best universe fit of any lead this week. Also had no
  reachable price history, so no trigger could have been built regardless.
- **GTLB — GitLab Inc.** (NASDAQ, USD). $41.67, cap $7.04B (core), ~$226M ADV. Q2 FY2027 reported as 2026-09-01 (Businesswire,
  2026-08-18); both `businesswire.com` and `ir.gitlab.com` blocked. Sits ~20% below its 52-week high — a drawdown, not a setup.
- **ASAN — Asana, Inc.** (NYSE, USD). $9.58, cap $2.28B (core), ~$58M ADV. Q2 FY2027 reported as 2026-09-03. ~39% below the 52-week
  high; no evidence of a base to work with.
- **AT1.DE — Aroundtown SA** (XETRA, EUR). €2.05, cap €2.12B (core). Carried from this morning's run, which recorded the 2026-08-26 H1
  report as verified; `www.aroundtown.de` refused at the gateway this run, so it reverts to `secondary_only`. Liquidity is marginal at
  ~€5.4M average daily value, barely over the non-US floor, and the name is absent from `eu_quote_history.csv` — so no structure either.

### Failed on substance as well — lower priority

- **DHER.DE — Delivery Hero SE** (XETRA, EUR). €36.83, cap €11.19B (larger-cap exception). The ten-session high-low range is **1.5% of
  price** — an extreme coil. Read in isolation that is a setup; in context it is a stock pinned by the pending Uber business-combination
  process. A break from a deal-pinned range is a deal-terms event, and no structural stop prices headline gap risk. Rejected on merit,
  not only on the blocked IR page.
- **AFX.DE — Carl Zeiss Meditec AG** (XETRA, EUR). €32.48, cap €2.84B (core), ~€9.8M ADV. Strongest structure in the reachable EU set,
  but fiscal year-end is September and no event falls inside the 42-day horizon. Price structure alone is a reason to research, not to
  enter. Still 25.5% below the 52-week high — a bounce inside a downtrend.
- **NEM.DE — Nemetschek SE** (XETRA, EUR). €65.75, cap €7.59B (core), ~€18.4M ADV. +15.0% over 20 sessions but 2.9% *below* the 20-day
  high and 29.5% below the 52-week high. No catalyst in horizon. This is the "it went up" rejection in `SETUPS.md`.
- **SHL.DE — Siemens Healthineers AG** (XETRA, EUR). €40.20, cap €44.99B ≈ $48.9B. The only core-tape name closing above its 20-day
  high, but by 0.2% — and its size sits at the very top of the exception bucket, close enough to the $50B exclusion that a rally
  removes it from the universe. No superior setup to justify the exception.
- **DOCU — DocuSign, Inc.** (NASDAQ, USD). $62.00, cap $11.84B (larger-cap exception). Date from FMP's calendar (tier-2 market data,
  not primary). ~28% below the 52-week high. An exception name needs to beat the core names it displaces; it does not.
- **ZM — Zoom Communications, Inc.** (NASDAQ, USD). $107.43, cap $31.5B (larger-cap exception). Reports 2026-08-25 per FMP. Near its
  52-week high, but two days to the print leaves no room to trigger, confirm and still honour the default `exit_before_catalyst = yes`.
- **BILI — Bilibili Inc.** (NASDAQ ADR). Surfaced only as an FMP calendar row for 2026-08-27. `ASIA_POLICY: exceptional-only` and
  nothing here is exceptional; ADR rather than local listing.
- **EVT.DE — Evotec SE** (XETRA, EUR). H1 2026 already published **2026-08-13** — the event is in the past. Independently fails
  liquidity at ~€2.7M average daily value, below even the conditional non-US floor.

### Excluded by the universe overlay

- **HOOD — Robinhood Markets, Inc.** (NASDAQ, USD). The single canonical snapshot row and the only fresh scanner observation, so the
  process is under pressure to make something of it. Current cap is **$97.2B**, roughly double the $50B exclusion — no scanner score
  promotes it. Worth noting the snapshot is already stale in the direction that hurts: it carries $106.23 as of 2026-08-21 while the
  live profile shows $108.13 after a further +13.70% session. `trades.csv` row 2026-0020 records a prior HOOD trade taken with no
  research trail; the routine has now rejected the name in four consecutive reports.

## Removed or expired

Nothing removed. No active rolling recommendation existed to remove, and no new object was admitted to the registry.

## Sources used

No tier-1 primary sources were opened this run — that is the central finding, not an omission.

- **Structured market data:** FMP MCP `company/profile-symbol` for S, GTLB, ASAN, DOCU, ZM, HOOD, AT1.DE, DHER.DE, AFX.DE, NEM.DE,
  SHL.DE; FMP MCP `calendar/earnings-calendar` for 2026-08-24 → 2026-09-13.
- **Repository:** `data/research_snapshot.csv` (rebuilt and archived this run), `data/eu_quote_history.csv`, `data/scanner_signals.csv`,
  `data/recommendations.csv`, `data/recommendation_reviews.csv`, `trades.csv`, `data/proposals.csv`, prior candidate reports and
  manifests.
- **Reputable secondary (leads only, cannot verify dates):** WebSearch summaries covering SentinelOne / GitLab / Asana reporting dates,
  Evotec and Grand City Properties H1 timing, and the Deutsche Börse index-review cadence.
- **Attempted and refused:** aroundtown.de, ir.deliveryhero.com, investors.asana.com, ir.gitlab.com, businesswire.com, sec.gov,
  data.sec.gov, deutsche-boerse.com, finance.yahoo.com, eqs-news.com, stockanalysis.com, en.wikipedia.org.

## What would unblock next week

1. Allow the research session outbound access to issuer IR domains, `sec.gov`/`data.sec.gov` and the exchange/index calendars, or run
   the weekly routine in an environment whose network policy permits them. Without this the routine can only ever produce
   NO NEW TRADE.
2. Fix or re-point the scanner: 39 total signal rows with a nine-day gap cannot support `PRELIMINARY_SCAN_COUNT: 12`.
3. Wire `data/eu_quote_history.csv` into `build_research_snapshot.py` as a market source. It already contains what the last three
   reports said was missing for European names.
4. Optional: an FMP plan covering `quote/*` and `chart/*`, or a Quartr Pro seat, would restore structured price history and
   issuer-document access independently of the egress policy.
